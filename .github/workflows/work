     name: Deploy Astro site to Pages
     on:
       push:
         branches: [main]
       workflow_dispatch:
     jobs:
       build:
         uses: withastro/action/.github/workflows/deploy.yml@v1
         with:
           node-version: 20
